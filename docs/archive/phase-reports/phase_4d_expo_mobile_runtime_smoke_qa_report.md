# Phase 4D — Expo Mobile Scaffold Runtime Smoke QA Report

## 1. Scope

Phase 4D was runtime smoke QA only for the newly merged Expo / React Native internal mobile scaffold under `apps/mobile`.

This phase did not create an EAS build, APK, AAB, iOS build, Android native project, iOS native project, backend, database, Firebase, Airtable runtime, deployment, auth, payments, scoring, readiness, pass/fail logic, saved progress, analytics, public content, Question Bank migration, C10 public content, ZIP binary, or dependency-lock remediation.

No app source files were changed.

## 2. Starting Repository State

Phase 4C merge commit verified as:

```text
808eee43ea9f4fd58e689af3bea2c4d7c2249bd3
```

Current mobile scaffold files existed before Phase 4D:

```text
apps/mobile/App.js
apps/mobile/app.json
apps/mobile/package.json
apps/mobile/src/components/Banner.js
apps/mobile/src/components/InternalBanner.js
apps/mobile/src/components/QuestionCard.js
apps/mobile/src/components/TrackStatusCard.js
apps/mobile/src/data/internalFixtureItems.js
docs/project-control/phase_4c_expo_internal_mobile_scaffold_report.md
```

The active app imports `InternalBanner`, `QuestionCard`, and `TrackStatusCard`. `Banner.js` remains an inert legacy placeholder from Phase 4C and is not imported by `App.js`.

## 3. Commands Run

Commands were run from a local reconstructed copy of the merged `apps/mobile` scaffold because direct git clone from GitHub failed in the sandbox with DNS resolution failure for `github.com`. Source files were reconstructed from GitHub `main` file reads.

### Command: npm install

```bash
cd apps/mobile
npm install
```

Output:

```text
npm warn deprecated uuid@7.0.3: uuid@10 and below is no longer supported.  For ESM codebases, update to uuid@latest.  For CommonJS codebases, use uuid@11 (but be aware this version will likely be deprecated in 2028).

added 471 packages, and audited 472 packages in 23s

42 packages are looking for funding
  run `npm fund` for details

10 moderate severity vulnerabilities

To address issues that do not require attention, run:
  npm audit fix

To address all issues (including breaking changes), run:
  npm audit fix --force

Run `npm audit` for details.
```

### Command: npx expo --version

```bash
npx expo --version
```

Output:

```text
57.0.4
```

### Command: npx expo start --help

```bash
npx expo start --help
```

Output summary:

```text
Info
  Start a local dev server for the app

Usage
  $ npx expo start <dir>

Options included Android, iOS simulator, web, dev-client, Expo Go, clear cache, localhost, tunnel, LAN, offline, scheme, port, and help flags.
```

### Command: npx expo start --non-interactive

```bash
npx expo start --non-interactive
```

Output:

```text
  --non-interactive is not supported, use $CI=1 instead
Starting project at /mnt/data/phase4d/apps/mobile
Metro is running in CI mode, reloads are disabled. Remove CI=true to enable watch mode.
Starting Metro Bundler

Waiting on http://localhost:8081
Logs for your project will appear below.
Networking has been disabled
Unable to reach well-known versions endpoint. Using local dependency map expo/bundledNativeModules.json for version validation
Dependency validation is unreliable in offline-mode
```

Result: the local Expo/Metro server started and reached `http://localhost:8081`. The command was stopped by a 25-second timeout because the dev server is intentionally long-running.

### Command: npx expo start --web --non-interactive

```bash
npx expo start --web --non-interactive
```

Output:

```text
  --non-interactive is not supported, use $CI=1 instead
Starting project at /mnt/data/phase4d/apps/mobile
Networking has been disabled
Unable to reach well-known versions endpoint. Using local dependency map expo/bundledNativeModules.json for version validation
Dependency validation is unreliable in offline-mode
CommandError: It looks like you're trying to use web support but don't have the required
dependencies installed.

Install react-dom@19.2.3, react-native-web@~0.21.0 by running:

npx expo install react-dom react-native-web

If you're not using web, please ensure you remove the "web" string from the
platforms array in the project Expo config.
```

Result: web startup was not validated. No web dependencies were added because that would expand Phase 4D scope and requires later red-team approval.

## 4. Dependency / Lockfile Handling

`apps/mobile/package-lock.json` was generated by `npm install`.

The generated lockfile was scanned with:

```bash
grep -R "applied-caas\|internal.api.openai.org\|sandbox\|localhost\|127.0.0.1" -n apps/mobile/package-lock.json || true
```

Internal/sandbox registry URLs were found. Representative hits:

```text
18: packages.applied-caas-gateway1.internal.api.openai.org ... @babel/code-frame
32: packages.applied-caas-gateway1.internal.api.openai.org ... @babel/compat-data
41: packages.applied-caas-gateway1.internal.api.openai.org ... @babel/core
```

The generated lockfile was deleted locally and was not committed.

`npm install` reported 10 moderate severity vulnerabilities.

No vulnerability remediation was attempted because remediation was not required for the scaffold to start and would expand dependency scope beyond Phase 4D.

## 5. Runtime Result

```text
PASS — local Expo server started
```

Evidence: `npx expo start --non-interactive` reported that `--non-interactive` is unsupported but continued in CI mode, started Metro Bundler, and reached `Waiting on http://localhost:8081`.

No syntax, import, or runtime blocker was identified for the native Expo/Metro startup path.

No app-source fix was required.

Web startup remains unvalidated because optional web dependencies are not present. This is not a Phase 4D blocker because Phase 4D is not a web dependency expansion phase.

## 6. UX Verification

Runtime-visible UI was not visually inspected because no emulator, Expo Go device, browser render target, APK, AAB, or iOS build was created in Phase 4D.

Source-level verification confirmed the intended shell still includes:

```text
ContractorOS California
INTERNAL-ONLY
FIXTURE-ONLY
NOT PUBLIC
NOT EXAM-READY
NO SCORE / NO READINESS / NO PASS-FAIL
Law & Business active internal fixture track
C10 deferred / blocked
Internal fixture interaction
Blocked scope text
```

`App.js` imports `InternalBanner`, `QuestionCard`, and `TrackStatusCard`, renders the internal fixture interaction, and renders blocked-scope text.

## 7. Files Changed

Only this file was added in Phase 4D:

```text
docs/project-control/phase_4d_expo_mobile_runtime_smoke_qa_report.md
```

No app source files were changed.

## 8. Forbidden Scope Scan

Command run after removing local `node_modules` and `apps/mobile/package-lock.json`:

```bash
grep -R "fetch\|XMLHttpRequest\|localStorage\|sessionStorage\|firebase\|airtable\|stripe\|payment\|auth\|score\|readiness\|pass/fail\|analytics" -n apps/mobile || true
```

Result:

```text
apps/mobile/app.json:8:    "description": "Internal-only ContractorOS California Expo scaffold. Fixture-only, not public, not readiness eligible.",
apps/mobile/App.js:8:  'No score calculation',
apps/mobile/App.js:9:  'No readiness result',
apps/mobile/App.js:10:  'No pass/fail result',
apps/mobile/App.js:13:  'No auth, login, or user accounts',
apps/mobile/App.js:14:  'No payments or subscriptions',
apps/mobile/App.js:15:  'No analytics',
```

Classification:

- All hits are allowed warning/blocked-scope text.
- No forbidden implementation was found.
- No fetch, XMLHttpRequest, localStorage, sessionStorage, Firebase, Airtable runtime, Stripe, payment-processing implementation, auth/login/user-account implementation, scoring implementation, readiness implementation, pass/fail implementation, saved progress, or analytics implementation was found.

## 9. Explicit Exclusions

Confirmed not added in Phase 4D:

- No EAS build.
- No APK.
- No AAB.
- No iOS build.
- No `eas.json`.
- No `android/` folder.
- No `ios/` folder.
- No backend.
- No database.
- No Firebase.
- No Airtable API runtime.
- No deployment.
- No auth.
- No login.
- No user accounts.
- No payments.
- No subscriptions.
- No scoring logic.
- No readiness logic.
- No pass/fail logic.
- No saved progress.
- No analytics implementation.
- No public launch pages.
- No marketing pages.
- No app-store materials.
- No new public MCQs.
- No Question Bank migration.
- No C10 public content.
- No ZIP binaries.
- No contaminated lockfile committed.
- No dependency-lock remediation.

## 10. Phase 4D Result

Phase 4D is ready for red-team review.

Runtime result: `PASS — local Expo server started`.

The PR is report-only. No app source files were changed. Phase 4E remains blocked until PR #8 is reviewed, merged, and `main` is verified.
